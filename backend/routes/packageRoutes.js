const express = require('express');
const router = express.Router();

// const { handlePackageCreation } = require('../controllers/authController');

// router.post('/create-package', handlePackageCreate);
// router.put('/update-package', handlePackageUpdate);
// router.delete('/delete-package', handlePackageDelete);

module.exports = router;
